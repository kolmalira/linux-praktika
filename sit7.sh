echo "для просмотра документации используется команда man"
echo "man grep"
man grep
echo ""
echo " -i, --ignore-case Ignore case distinctions in patterns and input data, so that characters that differ only in case match each other."
echo "для игнорирования регистра кoм grep используется ключ -r"

